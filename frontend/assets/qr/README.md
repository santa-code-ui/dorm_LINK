Generated hostel-gate QR PNGs go here. `backend/utils/QrUtil.gateUrl(gateCode)`
returns the URL each sticker should encode; render it with any QR library and save
the PNG in this folder (e.g. BLOCK-A-MAIN.png).
